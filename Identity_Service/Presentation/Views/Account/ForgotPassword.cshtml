@model Presentation.Models.ResetPassword.ForgotPasswordDTO
@{ ViewData["Title"] = "فراموشی رمز عبور"; }
<div class="global-container">
    <div class="card login-form">
        <div class="card-body">
            <h3 class="card-title text-center">فراموشی رمز عبور</h3>
            <div class="card-text text-right">
                <form id="login" asp-action="ForgotPassword" asp-antiforgery="true" method="post">
                    <div class="form-group">
                        <label for="exampleInputEmail1">نام کاربری</label>
                        <input asp-for="UserName" class="form-control form-control-sm" aria-describedby="emailHelp">
                        <span asp-validation-for="UserName" class="text-danger"></span>
                    </div>
                    @*<div class="form-group">
                            <div class="form-group" id="imgDiv" style="position: relative;float: right;height: 75px;width: 100px;background: url(/ReCaptcha/reCaptcha/@(Model.sessionId))">
                                <img onclick="reCaptcha()" src="~/Images/refresh.png" class="bg-soft-info spin-anything" style="width:20px;height:20px;bottom:5px;left:5px;position:absolute;" alt="بارگزاری مجدد" />
                            </div>
                            <div class="col-lg-4 form-group" style="float:right">
                                <div class="col-md-12">
                                    <label>کد امنیتی</label>
                                </div>
                                <div class="col-md-12">
                                    <input autocomplete="off" class="form-control pl-5" data-val="true" data-val-required="کد امنیتی را وارد کنید" id="txtCaptcha" name="txtCaptcha" type="text">
                                    <span class="field-validation-valid text-danger" data-valmsg-for="txtCaptcha" data-valmsg-replace="true"></span>
                                </div>
                            </div>
                        </div>*@

                    <button type="submit" class="btn btn-primary btn-block">ارسال ایمیل بازیابی رمز عبور</button>
                    <div class="sign-up">
                        <a asp-action="Login">ورود</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
@*<partial name="_ValidationScriptsPartial" />*@
@section scripts{
    @*<script>
            function reCaptcha() {
            $.ajax({
                type: "GET",
                url: '/ReCaptcha/captcha/',
                data: {id:@Model.sessionId},
                success: function (response) {
                    var d = new Date();
                    var newImage = '/ReCaptcha/reCaptcha/?id=@(Model.sessionId)&a=' + d.getMinutes() + d.getSeconds() + d.getMilliseconds();
                    document.getElementById('imgDiv').style.background = "url(" + newImage+")"
                },
                error: function(xhr) {
                    var err = eval("(" + xhr.responseText + ")");
                    alert(err.Message);
                }
            });
        }
        </script>*@
}
